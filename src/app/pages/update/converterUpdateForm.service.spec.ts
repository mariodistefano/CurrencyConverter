/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ConverterUpdateFormService } from './converterUpdateForm.service';

describe('Service: ConverterUpdateForm', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ConverterUpdateFormService]
    });
  });

  it('should ...', inject([ConverterUpdateFormService], (service: ConverterUpdateFormService) => {
    expect(service).toBeTruthy();
  }));
});
